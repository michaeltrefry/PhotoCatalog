{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Local LensWorks window. Files and jobs are handled by bounded Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
